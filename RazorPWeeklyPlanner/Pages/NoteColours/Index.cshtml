@page
@model RazorPWeeklyPlanner.Pages.NoteColours.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NoteColourCategory[0].Colour)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.NoteColourCategory) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Colour)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.NoteColourCategoryId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.NoteColourCategoryId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.NoteColourCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
